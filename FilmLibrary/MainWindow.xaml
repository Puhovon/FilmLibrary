<Window x:Class="FilmLibrary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FilmLibrary"
        xmlns:vm ="clr-namespace:FilmLibrary.ViewModel"
        xmlns:fa="http://schemas.fontawesome.com/icons/"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.ColumnSpan="1" Grid.Row="0">
            <Menu DockPanel.Dock="Top">
                    <MenuItem Header="Файлы">
                    <MenuItem Header="Выход" Command ="{Binding CloseApplicationCommand}"/>
                </MenuItem>
            </Menu>
        </DockPanel>
        <DataGrid Grid.Column="0"
                  AutoGenerateColumns="True"
                  Margin="0,18,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=FilmName}"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>
